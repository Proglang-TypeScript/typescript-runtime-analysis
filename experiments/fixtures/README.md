# Calculator fixture

This first-party fixture represents a public CommonJS function and its own tests.

```javascript
var calculate = require('./module');
if (calculate(10, 20) !== 30) throw new Error('Incorrect README example');
```
